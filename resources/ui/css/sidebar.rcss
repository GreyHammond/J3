body {
    height: 100vh;
    padding: 0;
}

.sidebar {
    background-color: #FFFFFF0C; /* white with 5% alpha for overlay effect */
    color: #D9D9D9;
    width: 64px;
    border-radius: 4px;
    padding: 0;
    position: fixed;
    top: 15px;
    left: 15px;
    bottom: 15px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    align-items: center;
    backdrop-filter: drop-shadow(#000000 0 4px 2px);
    transition: all 0.3s quintic-out;
}

.sidebar:hover {
    width: 200px;
}

.sidebar:hover .sidebar-main .sidebar-item:not(.selected) {
    width: 200px;
}

.sidebar:hover .sidebar-main .sidebar-item {
    width: 197px;
}

h1 {
    color: #CE3A3A;
    margin: 15px 15px 15px 14px;
    font-weight: 900;
    font-size: 32px;
    font-style: italic;
    filter: drop-shadow(#0000003F 0 4px 2px);
}

.sidebar-item {
    width: 64px;
    height: 64px;
    font-size: 24px;
    font-weight: normal;
    display: flex;
    align-items: center;
    transition: all 0.3s quintic-out;
}

.selected {
    background-color: #CE3A3A14;
    border-left: 3px #CE3A3A;
    width: 61px;
}

.sidebar-item:last-child {
    margin-top: auto;
}

.sidebar-item input {
    width: 24px;
    height: 24px;
    padding: 20px;
}

.sidebar-main {
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
}

#sidebar-home {
    decorator: image("../../textures/home.png" scale-none);
}

#sidebar-mods {
    decorator: image("../../textures/mod.png" scale-none);
}

#sidebar-archives {
    decorator: image("../../textures/archive.png" scale-none);
}

#sidebar-versions {
    decorator: image("../../textures/version.png" scale-none);
}

#sidebar-settings {
    decorator: image("../../textures/settings.png" scale-none);
}